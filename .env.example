# Copy to .env and adjust. docker compose reads this file automatically.
PLANTA_URL=https://planta.example.com/
# Directory on the host with your reference CSV files (mounted read-only at /data/reference)
REFERENCE_DIR=./examples/reference-files
